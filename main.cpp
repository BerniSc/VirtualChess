#include <memory>

#include "chessInterface.hpp"
#include "figure.hpp"

int main(int argc, char** argv) {
    //std::unique_ptr<engine::ChessEngine> virtualChessEngine;
    //virtualChessEngine = std::unique_ptr<engine::ChessEngine>(new engine::ChessEngine);


    return 0;
}